import path from "path";
export const PROPERTIES_FILE = path.join(process.cwd(), "test_suite", "user.properties");
export const RESULTS_DIR = path.join( process.cwd(), `test_suite`, `results` );
export const TEST_DIR = path.join( process.cwd(), 'test_suite' );